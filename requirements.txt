# Pin scikit-learn / xgboost to the versions that load ice_model.pkl cleanly.
# If unpickling errors on deploy, match these to the versions you trained with.
streamlit>=1.40
scikit-learn==1.8.0
xgboost==3.4.0
joblib>=1.3
pandas>=2.0
numpy>=1.26
altair>=5.0